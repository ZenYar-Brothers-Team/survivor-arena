# F1-06 — босс и мини-босс первой карты: evidence

Дата: 2026-09-24. Scope: `field-001-start-R1`, packet F1-06 (IP-21), IDs BOSS-001,
MIDBOSS-001. Числа — approved [baseline v1](../../balance/field001-baseline-v1.md);
решения — [DECISION-0054 §8](../../decisions/0054-field001-autonomous-execution.md).
Статус — только [STATUS](../STATUS.md#field001-execution).

## Реализация

- `ProductionBosses.json` генерируется из baseline (генератор отказывает, если
  политика порога/последовательности/рывков не выражается runtime); `ProductionBossCatalog`.
- Enemy framework: inline boss attacks (`BossAttackData`, `OwnedAttacks`),
  `KeepAttackOrderOnPhaseChange`, `StrictHealthThreshold`, фазы без атак,
  `EnemyAttackProfile.FixedOrientation`, `EnemyMovementProfile.DashCount/FollowUpTelegraphSeconds`.
  `BossCombatController` переписан на явный текущий controller (прежняя политика —
  поведение по умолчанию). Null-safe ссылки на текущую атаку в runtime/debug.
- Появление (7:30/13:30), награды XP 60/150 и отсутствие Книги/гарантированного
  зелья — timeline/framework; привязка расписания — F1-08.
- **Открытый art gate:** тела BOSS-001/MIDBOSS-001, снаряды веера/кольца.

## Проверки 2026-09-24

| Проверка | Результат |
|---|---|
| `generate_field001_content.py --check` | UP TO DATE |
| .NET harness compile | 0 errors |
| .NET harness NUnit | 411/727 PASS; +4 теста F1-06 PASS; регрессий 0 (fixture boss тесты без изменений) |
| Unity EditMode/PlayMode | **NOT RUN** (нет Unity) |

Покрыто: данные обоих боссов; первый выстрел в 4.3 s, чередование веер(5)/кольцо(10),
кольцо от 0° независимо от прицела, start-to-start 3.6 s; ровно 50% — без фазы;
энрейдж внутри интервала не сбрасывает очередь и не ускоряет текущий интервал,
следующий — 2.88 s; мини-босс: первая пара через 5.5 s, стоит в telegraph, dash ×5,
второй рывок перенацеливается, третьего нет. Unity-only: spawn по hooks, HP/name UI,
pause/terminal/pool cleanup на сцене — framework-тесты IP-15, совместная проверка F1-08/09.
