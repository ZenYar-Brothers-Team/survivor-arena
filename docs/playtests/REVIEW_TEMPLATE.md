# Review <ID> / revision <N>

## Brief и сопоставимость

- Вопрос / исходный файл + OBS-NN:
- Report/run IDs, raw links, config hashes, commit/dirty:
- Fixture/production; character/field/meta, build, seeds и uncovered RNG:
- Число прогонов и различия условий; неизвестное явно:
- Quality: incomplete, dropped, unsupported, source coverage:

## Анализ

- Факты со ссылками на marker/counters и точной цитатой:
- Estimate: формула, inputs, единицы, assumptions (отдельно от measured):
- Design intent и полные карточки релевантных production IDs:
- Гипотеза, альтернативы, confidence и способ опровержения:
- Итог: proposal / no-change / insufficient-evidence:

## Proposal (только если обоснован)

- Revision и baseline hash; content ID, файл, JSON pointer, было → стало, units:
- Цель, ожидаемый эффект, риски, affected docs/tests и rollback:
- Механика: отдельное правило/DECISION до реализации:
- Approval: точная ссылка на решение пользователя, revision и выбранные строки; до ответа pending:
- Rejected/deferred строки не применяются. Изменившийся baseline/revision требует нового review:
- Applied evidence: фактический diff/commit/hash и tests (до применения пусто):

## Follow-up

- Сценарий и критерии оставить/откатить/исследовать:
- Новый report/OBS и различия условий (до выполнения «не выполнен»):
- Результат, связь с исходным OBS, оставшиеся ограничения:

Для no-change/insufficient-evidence не выдумывать patch, approval или повторный run. OBS можно оставить открытым после завершённого review.
