namespace Game.Enemy
{
    /// <summary>Gameplay classification for consumers; it does not infer behavior from an ID.</summary>
    public enum EnemyCategory
    {
        Ordinary,
        Boss,
        Traveler
    }
}
