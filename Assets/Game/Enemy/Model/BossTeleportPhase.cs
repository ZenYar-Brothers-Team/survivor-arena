namespace Game.Enemy
{
    /// <summary>Tracking counts continuous far time; Telegraphing shows the landing marker until the slam.</summary>
    public enum BossTeleportPhase
    {
        Tracking,
        Telegraphing
    }
}
